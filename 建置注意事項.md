# FinRL 建置注意事項

## Docker 精簡建置策略（`docker/Dockerfile.slim`）

`finrl/__init__.py` 會在載入套件時就主動 import `finrl.test`／`finrl.trade`／
`finrl.train`，這幾個模組會連帶拉進整套依賴（Alpaca、WRDS、Selenium 等），
即使範例腳本只用到 `YahooDownloader` 也一樣。已知的解法：

- **CPU 版 PyTorch 要先裝**，用
  `--index-url https://download.pytorch.org/whl/cpu`，避免抓到含 CUDA 的
  巨大 wheel（沒有 GPU 的環境下沒必要）。
- **`requirements.txt` 裡的 `TA-lib` 和 `elegantrl` 要過濾掉不裝**：
  `TA-lib` 需要編譯的 C 函式庫，`elegantrl` 會拉進
  `gym[Box2D]` → `pygame`，需要 SDL2 開發標頭才能編譯——這兩個範例腳本的
  import 路徑都用不到，硬裝反而容易建置失敗，逐一排查每個 transitive
  import 太花時間，直接濾掉這兩個最省事。濾掉之後另外補裝
  `pyportfolioopt`（範例腳本有用到，但沒在 `requirements.txt` 裡列出）。

**如果之後要新增用到 `TA-lib` 或 `elegantrl` 的程式碼，要另外處理這兩個依賴的
編譯環境，不能直接从 filtered 版本的 requirements 裝。**

## 訓練腳本已改成可續跑（`examples/FinRL_StockTrading_2026_2_train.py`）

原本 A2C／TD3／SAC（以及 DDPG、PPO）都是每次執行從頭訓練 20000 步，沒有任何
斷點續跑機制。已改成：訓練前先檢查
`TRAINED_MODEL_DIR/agent_<name>.zip` 是否已存在，存在就直接跳過、印出
「already trained, skipping」，不存在才重新訓練並儲存。

**這代表如果要強制重新訓練某個 agent，要先手動刪掉對應的
`agent_<name>.zip`，不然腳本會直接跳過不練。**

## `_get_backtest_numbers.py`（新增腳本）

用途：載入已訓練好的 A2C/DDPG/PPO/TD3/SAC 五個 agent，在 `trade_data.csv`
上跑回測並輸出數字結果。**執行前提是 `TRAINED_MODEL_DIR` 底下五個 agent 都已經
訓練完成**（也就是要先把訓練腳本跑完一輪），以及當前目錄要有
`train_data.csv`／`trade_data.csv`（訓練腳本執行過程會產生）。

## 建置與執行指令備忘

```powershell
docker build -f docker/Dockerfile.slim -t finrl-slim:local .
docker run --rm -v ${PWD}/results:/src/results finrl-slim:local
```

`docker/run_pipeline.sh` 是容器的預設進入點，會依序跑訓練與回測流程。
